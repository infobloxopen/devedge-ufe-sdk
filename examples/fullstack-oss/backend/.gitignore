# Build artifacts
bin/
dist/
*.exe
*.dll
*.so
*.dylib

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Dependency directories
vendor/
node_modules/

# Test coverage
coverage.out
*.test

# APX lock file (commit this)
# apx.lock

# --- devedge-sdk scaffold (F028) ---
# Generated, consumer-local implementation produced by `buf generate` (the SDK
# `local:` plugins). It is NOT part of the published API surface and is pinned to
# the SDK version; regenerate with `make generate`. Engine (gorm) deps live in
# this module's go.mod only — never in the SDK's, never in the public proto.
/gen/
